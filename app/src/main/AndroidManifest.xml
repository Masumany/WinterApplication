<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission
        android:name="android.permission.INTERNET"/>
    <application
        android:networkSecurityConfig="@xml/network_config"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WinterApplication"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterActivity"></activity>
        <activity android:name=".MineActivity"></activity>
        <activity android:name=".StructActivity"></activity>
        <activity android:name=".DirectorActivity"></activity>
        <activity android:name=".LoginActivity"></activity>
        <activity android:name=".StructArticle"></activity>
        <activity android:name=".MainContentActivity"></activity>
        <activity android:name=".StructContentActivity"></activity>
        <activity android:name=".DirectorContentActivity"></activity>
        <activity android:name=".SquareActivity"></activity>
        <activity android:name=".SquareContentActivity"></activity>
        <activity android:name=".MoreActivity"></activity>

    </application>

</manifest>